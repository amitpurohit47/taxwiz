spring:
    datasource:
        driver-class-name: org.postgresql.Driver
    jpa:
        hibernate:
            ddl-auto: update
            show-sql: true
            dialect: org.hibernate.dialect.PostgresSQLDialect
jwt:
    secret: ${JWT_SECRET}