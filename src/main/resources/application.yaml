spring:
    datasource:
        url: jdbc:postgresql://localhost:5432/taxwiz
        username: postgres
        driver-class-name: org.postgresql.Driver
    jpa:
        hibernate:
            ddl-auto: update
            show-sql: true
            dialect: org.hibernate.dialect.PostgresSQLDialect
