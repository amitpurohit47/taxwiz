spring:
    datasource:
        driver-class-name: org.postgresql.Driver
    jpa:
        hibernate:
            ddl-auto: create
            show-sql: true
            dialect: org.hibernate.dialect.PostgresSQLDialect
